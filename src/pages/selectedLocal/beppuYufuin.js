import Toolbar from "../../components/toolbar/toolbar";
import { useParams } from 'react-router-dom';
import { useNavigate } from "react-router-dom";

const BeppuYufuin = () =>{

    return(
        <div>

        <Toolbar/>
        <h2>벳부 유후인</h2>
        </div>
    )
}

export default BeppuYufuin;